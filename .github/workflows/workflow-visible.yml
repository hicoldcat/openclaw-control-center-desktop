name: Workflow Visibility Check

on:
  push:
    branches:
      - master
      - main
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Print workflow visibility message
        run: echo "GitHub Actions workflow is detected and running."
